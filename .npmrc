registry=https://registry.npmjs.org/

# Ensure consistent dependency resolution
save-prefix=^

# Faster installs, safer defaults
fund=false
audit=false

# Optional: improve install performance
fetch-retries=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
